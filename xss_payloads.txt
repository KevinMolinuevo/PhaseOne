==================================================
SESSION 26: CLIENT-SIDE ATTACK PAYLOADS
==================================================

1. REFLECTED XSS
* Payload Used: <script>alert('XSS')</script>
* Concept: Why does this attack disappear when you refresh the page?
Answer: Reflected XSS disappears on refresh because the payload is not stored on the server. Each new page load starts fresh with no injected script.

2. STORED XSS (Cookie Theft)
* Cookie Value Stolen: admin_secret_99812_do_not_share
* Payload Used: <script>alert(document.cookie)</script>

3. CROSS-SITE REQUEST FORGERY (CSRF)
* Malicious URL Crafted: http://10.0.0.2:8082/transfer?to=Attacker&amount=5000
* Concept: What is the primary defense against CSRF?
Answer: The primary defense against CSRF is an anti-CSRF token — a unique, secret, per-session value embedded in forms that the server validates on each state-changing request, making it impossible for an attacker to forge a valid request.
